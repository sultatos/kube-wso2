apiVersion: v1
kind: Service
metadata:
  name: wso2apim-km-service
spec:
  selector:
    deploymentconfig: is-deployment
  ports:
  -
    name: servlet-http
    protocol: TCP
    port: 9763
  -
    name: servlet-https
    protocol: TCP
    port: 9443